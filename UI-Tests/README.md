## UI Tests – Solflare

- Framework: WebdriverIO + TypeScript + Mocha
- Browser: Chrome (local)

- Tests cover core wallet onboarding and basic wallet management flows
- Page Object pattern is used for better readability and maintainability

### Run
npm install  
npx wdio run wdio.conf.ts

- Tests are written to reflect real user behavior and UI state
